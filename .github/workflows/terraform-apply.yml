name: Terraform Apply

on:
  push:
    branches:
      - dev
      - homolog
      - prod
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

# concurrency:
#   group: terraform-${{ github.ref_name }}
#   cancel-in-progress: false

jobs:
  placeholder:
    name: Placeholder (${{ github.ref_name }})
    runs-on: ubuntu-latest

    steps:
      - name: Echo
        run: |
          echo "Repo: ${{ github.repository }} - Branch: ${{ github.ref_name }} - Commit SHA: ${{ github.sha }}"
